fix: Resolve static asset 404 errors with basePath configuration

- Update favicon and apple-touch-icon references to use CDN URLs
- Fix manifest path to include /mx basePath explicitly
- Update webmanifest files from UK to México branding
- Add vercel.json for root-level static file redirects in production
- Update middleware hreflang from en-gb to es-mx for MX market
- Simplify next.config.mjs by removing redundant rewrites
